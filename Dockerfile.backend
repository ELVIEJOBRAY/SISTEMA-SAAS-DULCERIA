FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

COPY requisitos.txt /app/requisitos.txt
RUN pip install --no-cache-dir -r /app/requisitos.txt

COPY . /app

EXPOSE 8000

CMD ["uvicorn", "nucleo.interfaz.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
